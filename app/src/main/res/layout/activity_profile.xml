<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    tools:context="com.example.selfie.app.ProfileActivity">

    <ImageView
        android:layout_width="200dp"
        android:layout_height="200dp"
        android:id="@+id/preview_selfie"
        android:layout_centerVertical="true"
        android:layout_centerHorizontal="true"
        android:scaleType="centerCrop"/>

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Pick"
        android:id="@+id/pick_button"
        android:onClick="onButtonClick"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/preview_selfie"
        android:layout_alignEnd="@+id/preview_selfie" />

    <Button
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Send"
        android:id="@+id/send_button"
        android:onClick="onButtonClick"
        android:layout_alignTop="@+id/pick_button"
        android:layout_alignLeft="@+id/preview_selfie"
        android:layout_alignStart="@+id/preview_selfie" />

</RelativeLayout>
